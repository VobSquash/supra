# Supra presentation — reference

Expand here when sections in [SKILL.md](SKILL.md) outgrow ~500 lines total or need copy-paste examples (feature folder skeletons, `BlocProvider` snippet, bottom sheet pattern).

For **what you liked in the old app** (greenfield / port), use [preserve.md](preserve.md) instead—keep that file as the living list.

## Analysis options

The app currently includes `package:flutter_lints/flutter.yaml`. When you switch to `very_good_analysis`, update `app/supra/analysis_options.yaml` accordingly and keep `page_width: 120` in formatter config.
