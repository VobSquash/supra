name: auth
description: Authentication orchestration and repository contracts; middleware composes with session_storage and client_supabase.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  meta: ^1.16.0
  session_storage:
    path: ../session_storage

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
