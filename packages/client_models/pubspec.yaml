name: client_models
description: DTOs + shared app config used by middleware and clients.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  intl: ^0.18.1
  path: ^1.9.0

dev_dependencies:
  build_runner: ^2.4.6
  freezed: ^2.5.8
  json_serializable: ^6.7.1
  lints: ^6.0.0
  test: ^1.25.6
